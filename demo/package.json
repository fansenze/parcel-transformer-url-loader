{
  "name": "demo",
  "version": "1.0.0",
  "main": "index.js",
  "author": "senze.fan",
  "license": "MIT",
  "scripts": {
    "deva": "rm -rf .cache && rm -rf build && parcel ./index.html -d build",
    "build": "rm -rf .cache && rm -rf build && parcel build ./index.html --no-cache -d build/ --public-url /"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "parcel-bundler": "^1.3.1",
    "parcel-plugin-url-loader": "^1.1.0"
  }
}
